{
  "manifest_version": 3,

  "name": "Skip the Scroll",
  "description": "Tired of scrolling through github issues to find that one magical response? Skip the scroll is a simple plugin that sorts github issue comments and allows you to iterate through them based on the social reactions.",
  "version": "1.0",

  "options_ui": {
    "page": "options.html"
  },

  "action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },

  "content_scripts": [
      {
          "matches": ["<all_urls>"],
          "js": ["js/vendor.js", "js/content_script.js"]
      }
  ],

  "permissions": [
    "storage"
  ],

  "host_permissions": [
    "<all_urls>"
  ]
}
